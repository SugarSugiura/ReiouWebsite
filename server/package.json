{
  "name": "auth0-express-webapp-sample",
  "scripts": {
    "build": "swc src --out-dir dist --source-maps",
    "watch": "npx swc server.ts -w -d dist --source-maps --log-watch-compilation",
    "parcel": "parcel src/server.ts"
  },
  "dependencies": {
    "debug": "~3.2.7",
    "dotenv": "^6.1.0",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "express-openid-connect": "^2.17.0",
    "http-errors": "~1.8.1"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.93"
  }
}
