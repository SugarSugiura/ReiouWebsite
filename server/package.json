{
  "name": "auth0-express-webapp-sample",
  "scripts": {
    "start": "node dist/server.js",
    "build": "swc src --out-dir dist --source-maps --copy-files",
    "watch": "swc src --watch --out-dir dist --source-maps --log-watch-compilation --copy-files"
  },
  "dependencies": {
    "debug": "~3.2.7",
    "dotenv": "^6.1.0",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "express-openid-connect": "^2.17.0",
    "http-errors": "~1.8.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.93",
    "chokidar": "^3.5.3"
  }
}
